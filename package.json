{
  "name": "song-recommendation-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm --workspace frontend run dev",
    "build": "npm --workspace frontend run build",
    "start": "npm --workspace frontend run start",
    "lint": "npm --workspace frontend run lint",
    "prisma:generate": "npm --workspace frontend run prisma:generate",
    "prisma:push": "npm --workspace frontend run prisma:push"
  }
}
