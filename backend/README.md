# Backend

This folder owns backend-specific project files.

- `prisma/schema.prisma` defines the database schema used by the app.
- The current API handlers still run through Next.js route handlers in `frontend/src/app/api` so the existing app behavior stays unchanged.

